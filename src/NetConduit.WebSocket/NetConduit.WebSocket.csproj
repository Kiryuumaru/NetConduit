<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<LangVersion>preview</LangVersion>
		<TargetFrameworks>net8.0;net9.0;net10.0</TargetFrameworks>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<IsAotCompatible>true</IsAotCompatible>
		<EnableTrimAnalyzer>true</EnableTrimAnalyzer>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<!-- Use local project reference by default; set to false for NuGet package release builds -->
		<UseLocalNetConduit Condition="'$(UseLocalNetConduit)' == ''">true</UseLocalNetConduit>
	</PropertyGroup>

	<!-- NuGet Package Metadata -->
	<PropertyGroup>
		<PackageId>NetConduit.WebSocket</PackageId>
		<Authors>Kiryuumaru</Authors>
		<Company>Kiryuumaru</Company>
		<Copyright>Kiryuumaru</Copyright>
		<Description>WebSocket transport helper for NetConduit stream multiplexer. Provides easy WebSocket client/server connection handling with automatic multiplexer setup.</Description>
		<PackageTags>multiplexer;stream;websocket;networking;async;aot</PackageTags>
		<PackageProjectUrl>https://github.com/Kiryuumaru/NetConduit</PackageProjectUrl>
		<RepositoryUrl>https://github.com/Kiryuumaru/NetConduit.git</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<PackageLicenseFile>LICENSE</PackageLicenseFile>
	</PropertyGroup>

	<ItemGroup>
		<None Include="..\..\README.md" Pack="true" PackagePath="\" />
		<None Include="..\..\LICENSE" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup Condition="'$(UseLocalNetConduit)' == 'true'">
		<ProjectReference Include="..\NetConduit\NetConduit.csproj" />
	</ItemGroup>

	<ItemGroup Condition="'$(UseLocalNetConduit)' != 'true'">
		<PackageReference Include="NetConduit" Version="1.1.3" />
	</ItemGroup>

</Project>
