# Failed Plans Index

| # | Plan | Category | Why It Failed |
|---|------|----------|---------------|
| 001 | SignalFlush before credit stall | Parameter | Negligible impact on credit wait |
| 002 | Lower grant threshold (1/4→1/8) | Parameter | More grants, same FlushLoop RTT |
| 003 | Auto-flush at 64KB pending | Parameter | No throughput improvement |
| 004 | Increase MaxCredits (4→8MB) | Parameter | More headroom, same RTT |
| 005 | Eager grant in EnqueueData (FlushLoop) | Structural | Grants still go through FlushLoop |
| 006 | Eliminate EnqueueData copy | Not attempted | Too complex, not the bottleneck |
| 007 | Direct-to-stream credits | Structural | `_streamLock` contention with data drains |
| 008 | Grant-on-receive + SignalFlush (BP check) | Structural | BP check negated change; game-tick -9% |
| 009 | Grant-on-receive (no BP check) | Structural | App reads fast — grant timing irrelevant |
| 010 | Idle-triggered flush | Structural | Not the bottleneck; game-tick -8% |
| 011 | Larger read buffer + adaptive flush + coalesce | Structural | Small-payload throughput regressed 3x; game-tick 50ch -12%; 1000ch failures |
| 012 | Pending-bytes flush + 32KB per-frame threshold | Parameter | Bulk throughput flat/neutral; 32KB threshold too conservative to matter |
| 014 | Accumulation-based flush threshold (256KB/128KB) | Structural | 1ch×1MB regressed -22% to -43%; SignalFlush scheduling delay hurts single-channel |
| 015 | Immediate credit grant flush on reader side | Structural | Reader ForceFlush contends with sender ForceFlush on _streamLock; 1ch×1MB -35% |
| 016 | Increase read PipeReader buffer (16KB→1MB) | Parameter | Re-evaluated: PASS — moved to succeeded_plans |
| 017 | Accumulation-based flush threshold (no SignalFlush fallback) | Structural | Re-evaluated: PASS — moved to succeeded_plans |
| 018 | Contention-adaptive flush (skip when _streamLock busy) | Structural | FlushLoop background contention makes CurrentCount unreliable; regression across all scenarios |
| 019 | Aggressive credit grant threshold (1/16 window) | Parameter | More credit frames add CPU overhead without reducing stall time; widespread regressions except 100ch×100KB |
| 020 | Direct-to-stream write for large frames | Structural | _streamLock contention with FlushLoop + two WriteAsync calls add more overhead than memcpy saves; single-channel -44% |
| 021 | Revert Plan 017 (keep 016 only) | Parameter | Per-frame ForceFlush adds more lock contention than accumulation delay; 1ch×100KB -58%, 10ch×100KB -45% |
| 022 | Increase accumulation threshold (256KB→1MB) | Parameter | Mixed results: 10ch×100KB +36% & 100ch×1MB +14% improved, but 10ch×1MB -9% & 100ch×100KB -15% regressed; no consistent gain |
| 024 | Signal FlushLoop for payload >= 1024 bytes | Parameter | 1ch×100KB +135% but 100ch×1KB -86% (per-frame signal threshold too low — overwhelms FlushLoop with many small-frame channels) |
| 025 | Signal FlushLoop on accumulated >= 65536 bytes | Parameter | 50ch×64B game-tick collapsed -85% (1.82x vs baseline 12.15x); signaling FlushLoop during active writes steals CPU and causes scheduling interference on 2-core pinning |
| 026 | Tiered ForceFlush with non-blocking try-drain at 65KB | Structural | Benchmark hung on 100ch tests; TryForceFlush holding _streamLock while acquiring _writeLock creates contention spiral; two concurrent lock orderings (_writeLock→_streamLock vs _streamLock→_writeLock) cause severe scheduling overhead |
| 027 | Reduce FlushInterval 1ms→250µs | Parameter | NC Mux crashes with InvalidOperationException (1ch×64B) and TimeoutException (1ch×256B); 250µs timer creates excessive FlushLoop wakeups causing contention/crash on 2-core pinned system; same crash pattern as Plan 028 |
| 028 | Remove frame-size guard from ForceFlush + reduce FlushInterval to 250µs | Structural | All NetConduit mux benchmarks throw InvalidOperationException/TimeoutException; removing toSend>=65536 guard causes ForceFlush from small-message writers, creating contention between caller-side drain and FlushLoop that corrupts pipe state; cascading failures broke even Raw TCP tests |
| 029 | ForceFlush on channel close | Structural | Bulk throughput regressed severely: 1ch×1KB -50%, 1ch×1MB -36%, 10ch×100KB -64%, 100ch×1KB -83%; ForceFlush in CloseAsync contends with FlushLoop (both compete for _writeLock and _streamLock); game-tick 1000ch×256B also regressed -42% |
| 030 | Lower ForceFlush accumulation threshold 256KB→128KB | Parameter | 1ch×1MB regressed 0.91x→0.40x (more frequent, smaller drains hurt single-channel); 100ch×100KB improved 0.22x→0.42x but gains don't offset single-channel losses; high inter-session variance complicates evaluation |
| 031 | Lower per-frame ForceFlush guard 64KB→16KB | Parameter | Mixed bulk ratios: 100ch×100KB improved 0.22x→0.51x, 1ch×100KB improved 0.27x→0.36x, but 10ch×100KB regressed 0.76x→0.31x; unchanged scenarios (1ch×1KB, 1ch×1MB) showed 26-33% variance confirming cross-session noise dominates; game-tick strong (20.14x 1ch×64B) |
| 032 | Reduce FlushInterval 1ms→500µs | Parameter | Regressions across board on 2-core pinning; shorter timer steals CPU from write/drain paths |
| 033 | Reduce FlushInterval 1ms→750µs | Parameter | Same regression pattern as 032; timer overhead too high on 2 cores |
| 034 | Adaptive FlushInterval (500µs–2ms based on throughput) | Structural | CPU waste from spinning on 2 cores; adaptation logic adds overhead |
| 035 | Reduce FlushInterval 1ms→250µs | Parameter | Worst timer reduction variant; same fundamental issue as 032-034 |
| 036 | Spin-wait in FlushLoop instead of timer | Structural | Burns one full core on 2-core system; throughput collapses |
| 037 | Signal-based flush on write completion | Structural | 1ch×1MB regression; signaling adds scheduling overhead on 2 cores |
| 038 | Hybrid signal+timer flush | Structural | Same regression as 037; signal overhead dominates |
| 039 | Conditional signal flush (large writes only) | Structural | Same regression as 037-038; any signaling hurts on 2 cores |
| 040 | Pipe segment coalescing (minimumSegmentSize 512KB) | Structural | Game-tick excellent but bulk catastrophic; large segments hurt TCP flow control and cause write stalls |
| 041 | Non-blocking drain in ForceFlush (fallback to SignalFlush) | Structural | Game-tick improved (+6-29%) but bulk 1ch×1MB -37% (0.57x vs 0.91x); SignalFlush fallback adds scheduling overhead; FlushLoop competes for _streamLock even in single-channel |
| 042 | Non-blocking drain in FlushLoop (inverse of 041) | Structural | Game-tick improved (+1-42%), 1000ch×64B now works (was FAILED). Bulk 1ch×1MB -58% (0.38x vs 0.91x); skipping FlushLoop drain breaks pipeline overlap; worse than 041 for single-channel |
| 043 | Post-write ForceFlush for writes >= 512B | Structural | 1ch x 1KB +137% but 100ch x 1KB -78%, 1ch x 100KB -37%; ForceFlush on every >= 512B write destroys FlushLoop batching for multi-channel |
| 044 | Post-write ForceFlush for small solo writes (16KB cap, solo-writer guard) | Structural | 1ch x 1KB -46% (0.13x vs 0.24x), 10ch x 1KB -64%; solo-writer detection via _unflushedDataBytes insufficient; ForceFlush contends with FlushLoop |
| 045 | Signal flush on empty-to-non-empty pipe transition | Structural | 1ch x 1MB -60% (0.36x vs 0.91x); signal fires after every FlushLoop commit; rapid small drains destroy batch efficiency |
| 046 | ForceFlush on first write to empty pipe for bulk frames | Structural | ForceFlush contention with FlushLoop; bulk 1ch x 1MB -45%; detecting empty pipe unreliable under concurrent writes |
| 047 | CAS idle-to-active state for FlushLoop fast-path | Structural | CAS transitions create scheduling overhead on 2 cores; FlushLoop state machine adds complexity without reducing latency |
| 048 | Conditional signal flush with solo-writer + payload threshold | Structural | Solo-writer detection via _activeWriterCount unreliable; signal contention on 2-core pinning; 100ch x 1KB -86% |
| 049 | ArrayPool drain coalescing in DrainPipeToStreamAsync | Structural | Extra buffer copy overhead exceeds TCP segment coalescing benefit; 1ch x 1MB -56%; ArrayPool contention under high concurrency |
| 050 | Inline writer-side non-blocking drain via TryInlineDrainAsync | Structural | _writeLock contention between writer inline CommitPipeWriter and FlushLoop; 1ch x 1MB -56%; 100ch x 1KB -80% |
| 051 | Solo-writer ForceFlush for medium frames (1024-65535B) via atomic counter | Structural | 1ch x 1KB -50% (0.12x vs 0.24x); 100ch x 1KB -88% (0.13x vs 1.10x); 1ch x 1MB -45% unexpectedly; Interlocked counter contention; ForceFlush overhead cancels timer elimination benefit |
| 052 | Inline commit + non-blocking TryDrainAsync | Unconditional CommitPipeWriter per-frame causes double-commit for large frames, -54% 1ch×1MB |
| 054 | Lower TryCommitAndDrain threshold from 8192 to 1024 | Structural | TryCommitAndDrain acquires _writeLock during Pipe commit, blocking concurrent writers in SendDataFrame; 10ch×1KB -58% (0.33x vs 0.79x); 10ch×100KB -49% (0.36x vs 0.70x); serializes multi-channel writes instead of allowing FlushLoop batching |
| 055 | Set TCP socket buffers to 2MB (SendBufferSize + ReceiveBufferSize) | Transport | Catastrophic bulk throughput regression: 1ch×100KB 0.10x (was 0.24x), 10ch×100KB 0.34x (was 0.70x), 1ch×1MB 0.45x (was 0.74x); 1000ch×64B game-tick TimeoutException; large kernel buffers remove natural backpressure causing FlushLoop batching inefficiency |
| 056 | Direct-to-stream write bypass when Pipe empty | Structural | Catastrophic game-tick regression: 1ch×64B 2.68x (was 15.69x), 50ch×64B 2.70x (was 10.68x); per-message syscalls eliminate Pipe batching advantage; small messages need batching, direct write only helps if frame > TCP segment size |
| 057 | Size-gated direct write bypass ≥1024B | Structural | Mixed results: 1ch×1MB improved (1.11x vs 0.74x), but 1ch×1KB regressed (0.11x vs 0.17x), 1000ch×256B TimeoutException, 1000ch×64B regressed (8.66x vs 10.79x); per-frame syscalls slower than Pipe batching for small-payload bulk; threshold cannot rescue small-frame throughput |
| 058 | Size-gated direct write bypass ≥65536B | Structural | Even with high threshold protecting game-tick and small bulk, large-frame direct writes lose Pipe's write-ahead pipelining: 1ch×1MB 0.56x (was 0.74x), 1ch×100KB 0.17x (was 0.24x), 10ch×100KB 0.41x (was 0.70x); only 100ch×1MB improved (0.69x vs 0.55x); Pipe isn't overhead — it's a performance-critical buffer keeping stream busy between writes |
| 059 | Per-frame commit + lock-free TryDrain + 65KB segments | Structural | Run 1 showed massive improvements (1ch×100KB 1.37x vs 0.24x, 1ch×1MB 1.11x vs 0.74x, 100ch×1KB 0.64x vs 0.19x) but 1000ch×64B TimeoutException; Run 2 systemically degraded due to extreme system instability (Raw TCP IOExceptions, Go FRP swinging 2.8x between runs); 1000ch×64B scored 8.80x vs baseline 10.79x; cannot reliably evaluate on unstable system |
| 060 | Per-frame commit + drain-only (no _writeLock in drain) + 65KB segments | Structural | Variant of Plan 059: commit inside _writeLock in SendFrameToWriter, new TryDrainOnlyAsync that only acquires _streamLock, same drain threshold (toSend >= 8192). Run 1: game-tick excellent (1ch×64B 19.46x, 1000ch×64B 11.91x), throughput mixed (1ch×100KB 0.41x vs 0.24x improved, but 1ch×1MB 0.35x vs 0.74x regressed). Run 2: 1000ch×64B TimeoutException (baseline passes reliably at 914K msg/s). Pipelining makes 1000ch fragile/borderline |
| 061 | Atomic _unflushedDataBytes tracking + decrement after drain + 65KB segments | Structural | Theory: _unflushedDataBytes grows monotonically causing unnecessary ForceFlush. Fix: Interlocked ops for thread safety, decrement after TryCommitAndDrain. Reality: decrement races with FlushLoop's Exchange(0) → _unflushedDataBytes goes negative → ForceFlush never triggers → throughput collapses. 1ch×100KB 0.14x (was 0.29x baseline), 1ch×1MB 0.52x (was 0.81x). Also 1000ch×64B TimeoutException. Negative counter prevents timely drain for large writes |
| 062 | Drain-until-empty loop + lower TryCommitAndDrain threshold to 1024 | Structural | Drain loop in DrainPipeToStreamAsync and TryCommitAndDrainAsync (while TryRead loop). Lower threshold from 8192 to 1024 for small-data throughput. Game-tick excellent (unchanged, 1ch×64B 19.59x, 50ch×64B 12.55x, 1000ch×64B 10.98x). Throughput mixed: 1ch×1MB improved 0.84x (was 0.74x), 100ch×1KB improved 0.35x (was 0.19x), but 10ch×100KB regressed 0.31x (was 0.70x). System degradation confounds results but absolute NC values close to today's baseline — changes appear neutral rather than improving |
| 063 | Reset _unflushedDataBytes in drain + CloseAsync immediate drain | Structural | Two changes: (1) reset _unflushedDataBytes=0 inside _writeLock after CommitPipeWriter in TryCommitAndDrainAsync (fixes monotonic accumulation causing unnecessary ForceFlush), (2) CloseAsync returns TryCommitAndDrainAsync instead of default (drain FIN immediately). Run 1: game-tick strong (1ch×64B 19.80x, 50ch×64B 13.59x, 1000ch×64B no timeout) but 1000ch×256B timeout. Run 2: excellent (1000ch×256B fixed at 515K, 1ch×64B 21.90x). Run 3: system degraded — Raw TCP 1000ch IOException, NC 1000ch×64B+256B both TimeoutException. Throughput extremely variable (±50-80% between runs). Changes neutral to slightly positive but 1000ch instability on degraded system causes Run 3 failure |
| 064 | Per-frame commit + drain-only TryCommitAndDrain | Structural | Minimal variant of 059/060: CommitPipeWriter inside _writeLock in SendFrameToWriter, removed _writeLock section from TryCommitAndDrainAsync (drain-only with _streamLock). No minimumSegmentSize change. Throughput improvements: 1ch×100KB 0.57x (was 0.24x), 10ch×1MB 0.91x (was 0.72x), 100ch×1MB 0.78x (was 0.55x). But NC 1000ch×64B TimeoutException in Run 1. Raw TCP also failed at 50ch×256B and 1000ch×64B with IOException. Per-frame commit overhead in game-tick path (73-byte writes) adds ~50ns per commit, not the cause — system instability at 1000ch persists across all plans |
| 065 | Size-gated per-frame commit (payload >= 8192) + drain-only | Structural | Gate per-frame commit at payload >= 8192 bytes so game-tick frames (73B, 265B) skip commit entirely — path identical to baseline. Bulk frames (65KB) get commit + TryDrainOnlyAsync (no _writeLock in drain). Runs 1-2: ALL game-tick passed including 1000ch (breakthrough — first time since Plan 059). Run 1: 1000ch×64B=1,146,571 (13.37x), Run 2: 1000ch×64B=852,123 (9.47x). Run 3: NC 1000ch×64B TimeoutException despite system degradation (Raw TCP FAILED at 1ch×256B, 10ch×256B, 50ch×64B). Throughput mixed: Run 3 best at 100ch×1MB 0.83x (was 0.55x). Size gate protects game-tick in normal conditions but 1000ch×64B still marginal under extreme system pressure |
| 066 | Larger pipe segments (65KB) + remove ForceFlush accumulation gate | Parameter | Two changes: (1) minimumSegmentSize 4KB→65536 for fewer drain syscalls, (2) remove `unflushed >= 262144` gate so every 64KB frame gets ForceFlush. Run 1: system severely degraded (5/8 Raw TCP Failed). NC 1000ch×64B TimeoutException. Throughput: 1ch×100KB improved 0.62x (was 0.24x) from guaranteed flush, but 1ch×1MB REGRESSED to 0.38x (was 0.74x) — ForceFlush per frame loses batching benefit (16× 65KB writes vs 4× 262KB writes). Removing accumulation gate harms large transfers |
| 067 | Larger pipe segments (65KB) + SpinWait(20) retry in TryCommitAndDrain | Parameter | Two changes: (1) minimumSegmentSize 65536, (2) SpinWait(20) retry after initial Monitor.TryEnter(_writeLock) fails in TryCommitAndDrainAsync. Theory: SpinWait overlaps with FlushLoop's ~50ns Phase 1 so retry succeeds → immediate drain. Run 1: game-tick excellent (1ch×64B 21.92x, 50ch×64B 12.04x) but NC 1000ch×64B TimeoutException (system degraded: Raw TCP 1000ch IOException). Throughput mixed: 10ch×100KB 0.81x (was 0.70x), 10ch×1MB 0.84x (was 0.72x) improved; 1ch×1MB 0.51x (was 0.74x) regressed. Same persistent 1000ch instability as Plans 061-066 |
| 069 | Lock-free RecordSend + remove EnableReconnection option (always-on) | Structural | Made RecordSend lock-free (removed lock, kept ring buffer copy) + removed EnableReconnection option + lazy ring buffer allocation. Lock removal helped single-channel (1ch×1MB: 436.6 vs 307.8 with lock) but ring buffer COPY causes cache thrashing on 2-core system. Bulk 1ch×100KB: 0.20x vs baseline 0.77x, 10ch×100KB: 0.61x vs 1.13x, 100ch×1MB: 0.73x vs 1.21x. Game-tick OK (21.57x 1ch×64B). The memcpy per-write evicts hot data from L1/L2 cache, degrading subsequent Pipe and socket operations. Lock was only part of the problem — the copy itself is the fundamental bottleneck |
| 070 | Zero-copy RecordSend (eliminate ring buffer entirely) | Structural | Removed ring buffer from ChannelSyncState; RecordSend = Interlocked.Add only; GetUnacknowledgedDataFrom returns empty. Benchmarked with EnableReconnection=false (same code path as baseline after change). NC absolute perf improved +20-47% (system variance) but Go FRP also measured ~50% faster than baseline session → ratios dropped. GT 1ch×64B: 18.25x vs 20.62x baseline, GT 50ch×64B: 10.53x vs 12.13x baseline. Failed RECON criteria: multiple ratios below 95% threshold. Also: EnableReconnection option not actually removed; 1000ch Mux hung with EnableReconnection=true (reconnection deadlock during disposal) |
| 071 | Remove EnableReconnection + ReconnectBufferSize=0 default (zero-overhead) | Structural | Removed EnableReconnection property, changed ReconnectBufferSize default from 1MB to 0. With bufsize=0, StartRecording is no-op and RecordSend takes Interlocked.Add-only fast path — byte-identical to baseline EnableReconnection=false. Fixed benchmark 1000ch hang (MaxAutoReconnectAttempts=1). All 528 tests pass. Build 0 warnings. NC absolute performance IMPROVED (+19-65% bulk, +27-32% game-tick), but Go FRP/Smux improved even more in this system session (+14-183%). Fundamental issue: system variance between baseline measurement session and current session makes RATIO comparisons fail even when NC code path is identical. Run 1: GT 1ch×64B 18.67x vs 20.62x (90.5%), Bulk 1ch×100KB 0.62x vs 0.77x (81%). Run 2: GT 1ch×64B 17.23x vs 20.62x (83.5%), Bulk 100ch×1MB 0.78x vs 1.21x (64%). Plan 071 proves that removing EnableReconnection with ReconnectBufferSize=0 has zero code-path impact, but ≥95% ratio retention cannot be met due to Go runtime performance variance across sessions |
| 072 | Re-baseline + Remove EnableReconnection (Plan 071 retry with fresh baseline) | Structural | Strategy: re-baseline docs/benchmarks.md with current session numbers first to eliminate cross-session Go variance, then apply identical Plan 071 code changes. Re-baseline succeeded (copied comparison-report.md → docs/benchmarks.md). Code changes: removed EnableReconnection property, ReconnectBufferSize default 1MB→0, updated all tests/samples/docs/benchmarks. Build 0W/0E, 528 tests pass. Run 1 FAILED: within-session variance exceeds 5% for multiple scenarios. NC absolute numbers dropped 14-17% from baseline in several game-tick configs (10ch×64B: 1,216,741→1,012,826). Bulk same pattern: 100ch×1KB 2.30x→0.95x, 10ch×1KB 1.06x→0.92x. Re-baseline strategy doesn't solve variance because baseline was single snapshot — within-session fluctuation is as bad as cross-session. Code path is provably identical (ReconnectBufferSize=0 → StartRecording no-op → RecordSend same Interlocked.Add fast path) |
