name: Nuke CICD Pipeline
on:
  push:
    branches:
    - prerelease
    - master
    tags:
    - bump-*
  pull_request:
    branches:
    - '**'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  PRE_SETUP:
    name: Pre Setup
    runs-on:
      labels: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        fetch-tags: true
        submodules: recursive
        persist-credentials: true
    - id: NUKE_RUN
      name: Run Nuke PipelinePreSetup
      run: chmod +x ./build.sh && ./build.sh PipelinePreSetup
    timeout-minutes: 30
    outputs:
      NUKE_PRE_SETUP: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP }}
      NUKE_PRE_SETUP_OUTPUT_TEST_MATRIX: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_OUTPUT_TEST_MATRIX }}
      NUKE_PRE_SETUP_OUTPUT_BUILD_MATRIX: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_OUTPUT_BUILD_MATRIX }}
      NUKE_PRE_SETUP_OUTPUT_PUBLISH_MATRIX: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_OUTPUT_PUBLISH_MATRIX }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_SUBMODULES }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CONDITION: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CONDITION }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUNS_ON: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUNS_ON }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUN_SCRIPT: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUN_SCRIPT }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_MAIN_RESTORE_KEY: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_DEPTH: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_DEPTH }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_TAGS: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_TAGS }}
      NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_SUBMODULES: ${{ steps.NUKE_RUN.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_SUBMODULES }}
  NET_CONDUIT_TEST:
    name: Test NetConduit
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_test
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_RUN_SCRIPT }} Run --args "net_conduit_test"
    - name: Upload net_conduit pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___net_conduit___NET_CONDUIT_TEST
        path: ./.nuke/temp/artifacts-upload/net_conduit/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___$common___NET_CONDUIT_TEST
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TEST_CONDITION == 'true'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_TCP_TEST:
    name: Test NetConduit.Tcp
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_tcp_test
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_RUN_SCRIPT }} Run --args "net_conduit_tcp_test"
    - name: Upload net_conduit_tcp pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___net_conduit_tcp___NET_CONDUIT_TCP_TEST
        path: ./.nuke/temp/artifacts-upload/net_conduit_tcp/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___$common___NET_CONDUIT_TCP_TEST
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_TEST_CONDITION == 'true'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_WEBSOCKET_TEST:
    name: Test NetConduit.WebSocket
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_websocket_test
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_RUN_SCRIPT }} Run --args "net_conduit_websocket_test"
    - name: Upload net_conduit_websocket pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___net_conduit_websocket___NET_CONDUIT_WEBSOCKET_TEST
        path: ./.nuke/temp/artifacts-upload/net_conduit_websocket/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common pre_test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: pre_test___$common___NET_CONDUIT_WEBSOCKET_TEST
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_TEST_CONDITION == 'true'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_BUILD:
    name: Build NetConduit
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_build
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_RUN_SCRIPT }} Run --args "net_conduit_build"
    - name: Upload net_conduit build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___net_conduit___NET_CONDUIT_BUILD
        path: ./.nuke/temp/artifacts-upload/net_conduit/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___$common___NET_CONDUIT_BUILD
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_TEST
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_BUILD_CONDITION == 'true' && needs.NET_CONDUIT_TEST.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_TCP_BUILD:
    name: Build NetConduit.Tcp
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit_tcp pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit_tcp___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_tcp_build
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_RUN_SCRIPT }} Run --args "net_conduit_tcp_build"
    - name: Upload net_conduit_tcp build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___net_conduit_tcp___NET_CONDUIT_TCP_BUILD
        path: ./.nuke/temp/artifacts-upload/net_conduit_tcp/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___$common___NET_CONDUIT_TCP_BUILD
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_TCP_TEST
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_BUILD_CONDITION == 'true' && needs.NET_CONDUIT_TCP_TEST.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_WEBSOCKET_BUILD:
    name: Build NetConduit.WebSocket
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit_websocket pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit_websocket___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_websocket_build
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_RUN_SCRIPT }} Run --args "net_conduit_websocket_build"
    - name: Upload net_conduit_websocket build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___net_conduit_websocket___NET_CONDUIT_WEBSOCKET_BUILD
        path: ./.nuke/temp/artifacts-upload/net_conduit_websocket/*
        if-no-files-found: error
        retention-days: 1
    - name: Upload common build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build___$common___NET_CONDUIT_WEBSOCKET_BUILD
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_WEBSOCKET_TEST
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_BUILD_CONDITION == 'true' && needs.NET_CONDUIT_WEBSOCKET_TEST.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_PUBLISH:
    name: Publish NetConduit
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit___*
    - name: Download net_conduit build artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: build___net_conduit___*
    - name: Download net_conduit post_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: post_test___net_conduit___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_publish
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_RUN_SCRIPT }} Run --args "net_conduit_publish"
    - name: Upload common publish artifacts
      uses: actions/upload-artifact@v4
      with:
        name: publish___$common___NET_CONDUIT_PUBLISH
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_TEST
    - NET_CONDUIT_BUILD
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_PUBLISH_CONDITION == 'true' && needs.NET_CONDUIT_TEST.result == 'success' && needs.NET_CONDUIT_BUILD.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_TCP_PUBLISH:
    name: Publish NetConduit.Tcp
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit_tcp pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit_tcp___*
    - name: Download net_conduit_tcp build artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: build___net_conduit_tcp___*
    - name: Download net_conduit_tcp post_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: post_test___net_conduit_tcp___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_tcp_publish
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_RUN_SCRIPT }} Run --args "net_conduit_tcp_publish"
    - name: Upload common publish artifacts
      uses: actions/upload-artifact@v4
      with:
        name: publish___$common___NET_CONDUIT_TCP_PUBLISH
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_TCP_TEST
    - NET_CONDUIT_TCP_BUILD
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_TCP_PUBLISH_CONDITION == 'true' && needs.NET_CONDUIT_TCP_TEST.result == 'success' && needs.NET_CONDUIT_TCP_BUILD.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  NET_CONDUIT_WEBSOCKET_PUBLISH:
    name: Publish NetConduit.WebSocket
    runs-on: ${{ fromJson(needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUNS_ON) }}
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_DEPTH }}
        fetch-tags: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_FETCH_TAGS }}
        submodules: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CHECKOUT_SUBMODULES }}
        persist-credentials: true
    - name: Download net_conduit_websocket pre_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: pre_test___net_conduit_websocket___*
    - name: Download net_conduit_websocket build artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: build___net_conduit_websocket___*
    - name: Download net_conduit_websocket post_test artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: post_test___net_conduit_websocket___*
    - name: Cache Run
      uses: actions/cache@v4
      with:
        path: ./.nuke/temp/cache
        key: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_KEY }}
        restore-keys: |-
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_RESTORE_KEY }}
          ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CACHE_MAIN_RESTORE_KEY }}
    - id: NUKE_RUN
      name: Run Nuke net_conduit_websocket_publish
      run: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_RUN_SCRIPT }} Run --args "net_conduit_websocket_publish"
    - name: Upload common publish artifacts
      uses: actions/upload-artifact@v4
      with:
        name: publish___$common___NET_CONDUIT_WEBSOCKET_PUBLISH
        path: ./.nuke/temp/artifacts-upload/$common/*
        if-no-files-found: error
        retention-days: 1
    needs:
    - PRE_SETUP
    - NET_CONDUIT_WEBSOCKET_TEST
    - NET_CONDUIT_WEBSOCKET_BUILD
    if: "! failure() && ! cancelled() && needs.PRE_SETUP.outputs.NUKE_PRE_SETUP_NET_CONDUIT_WEBSOCKET_PUBLISH_CONDITION == 'true' && needs.NET_CONDUIT_WEBSOCKET_TEST.result == 'success' && needs.NET_CONDUIT_WEBSOCKET_BUILD.result == 'success'"
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
  POST_SETUP:
    name: Post Setup
    runs-on:
      labels: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        fetch-tags: true
        submodules: recursive
        persist-credentials: true
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./.nuke/temp/artifacts-download
        pattern: '*___*'
    - name: Run Nuke PipelinePostSetup
      run: chmod +x ./build.sh && ./build.sh PipelinePostSetup
    needs:
    - PRE_SETUP
    - NET_CONDUIT_TEST
    - NET_CONDUIT_TCP_TEST
    - NET_CONDUIT_WEBSOCKET_TEST
    - NET_CONDUIT_BUILD
    - NET_CONDUIT_TCP_BUILD
    - NET_CONDUIT_WEBSOCKET_BUILD
    - NET_CONDUIT_PUBLISH
    - NET_CONDUIT_TCP_PUBLISH
    - NET_CONDUIT_WEBSOCKET_PUBLISH
    if: success() || failure() || always()
    timeout-minutes: 30
    env:
      NUKE_PRE_SETUP: ${{ needs.PRE_SETUP.outputs.NUKE_PRE_SETUP }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_TEST: ${{ needs.NET_CONDUIT_TEST.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_TCP_TEST: ${{ needs.NET_CONDUIT_TCP_TEST.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_WEBSOCKET_TEST: ${{ needs.NET_CONDUIT_WEBSOCKET_TEST.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_BUILD: ${{ needs.NET_CONDUIT_BUILD.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_TCP_BUILD: ${{ needs.NET_CONDUIT_TCP_BUILD.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_WEBSOCKET_BUILD: ${{ needs.NET_CONDUIT_WEBSOCKET_BUILD.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_PUBLISH: ${{ needs.NET_CONDUIT_PUBLISH.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_TCP_PUBLISH: ${{ needs.NET_CONDUIT_TCP_PUBLISH.result }}
      NUKE_RUN_RESULT_GITHUB_NET_CONDUIT_WEBSOCKET_PUBLISH: ${{ needs.NET_CONDUIT_WEBSOCKET_PUBLISH.result }}
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  NUKE_NUGET_AUTH_TOKEN: ${{ secrets.NUGET_AUTH_TOKEN }}
  NUKE_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
